import styled from "styled-components";

const GutterWrapper = styled.div`
    margin-left: calc(max(10px, (100% - 1370px) * 0.5));
    max-width: 1370px;
    width: 100%;
`;

export default GutterWrapper;
